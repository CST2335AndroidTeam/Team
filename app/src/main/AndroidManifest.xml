<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yu.team_project_1">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Weather"
            android:label="weather" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Garage" />
        <activity android:name=".AutomobileMainActivity" />
        <activity android:name=".DriveDetailActivity" />
        <activity
            android:name=".HouseTemp"
            android:label="@string/title_activity_house_temp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Living_Room"
            android:label="@string/title_activity_living__room"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ScheduleTimeTemp"
            android:label="@string/schedule"/>
        <activity android:name=".HouseMain"
            android:label="@string/title_activity_house_main"/>

    </application>

</manifest>