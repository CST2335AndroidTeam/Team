<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:textAlignment="textStart"
        android:textSize="30sp"
        android:text="FM XX.X"
        android:layout_marginStart="35dp"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/auto_edit_image"
        android:id = "@+id/station_name"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        android:layout_alignParentEnd="true"
        android:src="@drawable/auto_delete"
        android:id="@+id/auto_delete_image" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        android:src="@drawable/auto_edit"
        android:id="@+id/auto_edit_image"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@+id/auto_delete_image" />


</RelativeLayout>